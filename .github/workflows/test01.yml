name: Test Workflow

on:
    push:
        branches:
            - main
            - dev

    pull_request:
        branches:
            - main
            - dev
    
            

jobs:
    test-job:
        runs-on: ubuntu-latest
        
        steps:
            - name: this is my test step one
              run: echo "Hello from my first test job"  

    
    another-job:
        runs-on: ubuntu-latest
        
        steps: 
            - name: This is another job
              run: |
                echo "Hi From another Job"
                echo "It works correcthly"  
    

# jobs:
#     another-job:
#         runs-on: ubuntu-latest
        
#         steps:
#             - name: This is another job
#               run: |
#                 echo "This is anoter job"
#                 echo "Hi from another job"

    