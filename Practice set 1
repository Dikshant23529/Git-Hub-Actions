Question 1: Basic Structure
Write a complete workflow that:

Is named "Quiz Workflow 1"

Triggers on push to the main branch

Has one job called say-hello running on Ubuntu

Has a step that prints: "Quiz completed successfully!



Question 2: Multiple Jobs
Write a workflow that:

Is named "Quiz Workflow 2"

Triggers manually (workflow_dispatch)

Has TWO jobs: job-alpha and job-beta

Both run on Ubuntu

job-alpha prints: "I am the first job"

job-beta prints: "I am the second job"

Important: Both jobs should run at the same time (parallel).


Question 3: Workflow Dispatch with Inputs
Write a workflow that:

Is named "Quiz Workflow 3"

Triggers manually

Has two inputs:

username (string input, required, default: "Student")

difficulty (choice input, required, options: easy, medium, hard)

Has one job called greet

Steps:

Print: "Hello, [username]!" (using the input value)
Print: "You selected [difficulty] difficulty" (using the input value)


Question 4: Find the Errors
Below is a workflow with 4 errors. Find and list them:

name: Buggy Workflow
'''
on:
    push:
        branches:
            - main

jobs:
    test-job:
        runs-on: ubuntu:latest
        steps:
            - name: Test step
              run: echo "Testing"
    
    build-job:
        runs-on: ubuntu-latest
        steps:
            - name: Build step
              run: echo "Building"

on:
    workflow_dispatch:

'''


name: Complete Me

on:                                # What goes here?
  workflow_dispatch:
    inputs:                      # What keyword for inputs?
      my_input:
        description: 'Test input'
        required: true
        default: 'default value'  # What keyword for default?
        type: string

jobs:
  my-job:
    runs-on: ubuntu:latest            # Valid Ubuntu runner
    steps:
      - run: echo "${{ github.event.inputs.my_input }}"  # How to access input?



Question 7: Real-World Scenario
Create a workflow that simulates a deployment system:

Name: "Deployment Pipeline"

Manual trigger with:

environment (choice: staging, production, required)

version (string, required, default: "1.0.0")

Two jobs:

build (runs first, prints: "Building version [version]")

deploy (runs AFTER build, prints: "Deploying to [environment]")      